# AWS RDS Deployment Configuration
# Copy to .env and update with your AWS RDS details

# Domain Configuration
DOMAIN=yourdomain.com
LETSENCRYPT_EMAIL=admin@yourdomain.com

# AWS RDS PostgreSQL Configuration
# Create RDS instance first, then update these values
DATABASE_URL=postgresql://postgres:your_password@your-db-instance.abc123xyz.us-east-1.rds.amazonaws.com:5432/asset_management?sslmode=require

# Alternative individual components (if not using DATABASE_URL)
PGHOST=your-db-instance.abc123xyz.us-east-1.rds.amazonaws.com
PGPORT=5432
PGDATABASE=asset_management
PGUSER=postgres
PGPASSWORD=your_secure_password
PGSSLMODE=require

# Security Configuration
SESSION_SECRET=your_secure_random_session_secret_min_32_characters
NODE_TLS_REJECT_UNAUTHORIZED=1

# AWS RDS Specific Settings
# 1. Create RDS Parameter Group with these recommended settings:
#    - max_connections: 200
#    - shared_buffers: 256MB
#    - effective_cache_size: 1GB
#    - maintenance_work_mem: 64MB
#    - checkpoint_completion_target: 0.9
#    - wal_buffers: 16MB
#    - default_statistics_target: 100
#    - random_page_cost: 1.1
#    - effective_io_concurrency: 200
#    - work_mem: 4MB

# 2. Security Group Configuration:
#    - Inbound Rule: PostgreSQL (5432) from your server IP
#    - Outbound Rule: All traffic

# 3. Backup Configuration:
#    - Automated backups: 7 days
#    - Backup window: 03:00-04:00 UTC
#    - Maintenance window: Sun:04:00-Sun:05:00 UTC

# 4. Performance Insights:
#    - Enable Performance Insights
#    - Retention period: 7 days

# Deployment Command:
# docker compose -f docker-compose.ssl-external-db.yml up -d --build